# Application dependencies

require:
    - play
    - play-envers 1.0

repositories:
    - playCustomModules:
        type: local
        artifact: ${application.path}/../play-envers
        contains:
            - play-envers