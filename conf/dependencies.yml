# Application dependencies

require:
    - play
    - envers -> envers

repositories:
    - playCustomModules:
        type: local
        artifact: ${application.path}/../play-envers
        contains:
            - envers